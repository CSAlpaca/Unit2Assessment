<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h1>TO DO LIST</h1>
    <h3 id="NoTodos">
      <%= foundTodos.length > 0 ? "" : "There are no To Dos yet!" %>
    </h3>

    <ul>
      <% for(let i = 0; i < foundTodos.length; i++) { %>
      <li>
        <%= foundTodos[i].todo %> <span></span> <%= foundTodos.done? 'Done' :
        'Not Done' %> <% } %>

        <span></span>
      </li>
    </ul>

    <form action="/" method="POST">
      <input type="text" name="todo" name="done" required />
      <button>ADD TO DO</button>

      <br />
    </form>
  </body>
</html>
